import { expect, test } from 'vitest'

test('navigator guard', () => {
	// sessionStorage.setItem(TOKEN, '1245125.21414')
	// router.push({ name: LOGIN })
	// const currentRoute = router.currentRoute.value.name
	// expect(currentRoute).not(LOGIN)
	console.log('hi')
})
